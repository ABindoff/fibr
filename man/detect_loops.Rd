% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/detect_loops.R
\name{detect_loops}
\alias{detect_loops}
\title{Find approximate loops in base (hyperparameter) space}
\usage{
detect_loops(
  base_draws,
  epsilon = NULL,
  min_gap = 50L,
  k = 100L,
  max_loops = 5000L,
  scale = TRUE
)
}
\arguments{
\item{base_draws}{Numeric matrix [n_iter x n_base]. Each row is one
iteration; columns are the base-space (hyperparameter) variables.}

\item{epsilon}{Scalar tolerance for loop closure (in standardised units
when \code{scale = TRUE}). \code{NULL} auto-selects as the 5th percentile of a
sub-sampled pairwise distance distribution.}

\item{min_gap}{Minimum number of iterations between loop start and end
(default 50). Screens out autocorrelated neighbours.}

\item{k}{Number of nearest neighbours to query per point (default 100).
Should exceed the expected number of points within \code{epsilon}.}

\item{max_loops}{Maximum loop pairs returned (default 5000). When more
are found, the tightest (smallest distance) are kept.}

\item{scale}{Logical; standardise base variables before distance
computation so that \code{epsilon} is in units of posterior SDs (default TRUE).}
}
\value{
A data frame with columns \code{start}, \code{end}, \code{distance} (one row
per loop pair). Attribute \code{"epsilon"} records the tolerance used.
}
\description{
Scans a chain for pairs of iterations where the base-space parameters
return within \code{epsilon} of a previous position, separated by at least
\code{min_gap} steps.  These loop pairs are the raw material for estimating
the holonomy transport map.
}
\keyword{internal}
