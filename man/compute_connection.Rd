% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/connection.R
\name{compute_connection}
\alias{compute_connection}
\title{Compute the connection form of a hierarchical MCMC chain}
\usage{
compute_connection(
  chain,
  base_vars,
  fiber_vars,
  method = "analytic_glmm",
  stan_data = NULL,
  beta_vars = c("beta[1]", "beta[2]"),
  n_subsample = 500L
)
}
\arguments{
\item{chain}{A \code{\link[posterior:draws_array]{posterior::draws_array}} or named matrix (rows =
iterations, columns = parameters).}

\item{base_vars}{Character vector of base-space (hyperparameter) column
names; must be \code{c("mu", "sigma")} for \code{method = "analytic_glmm"}.}

\item{fiber_vars}{Character vector of fiber column names, e.g.
\code{paste0("alpha[", 1:8, "]")}.}

\item{method}{\code{"analytic_glmm"} (default and currently only option).}

\item{stan_data}{Named list; the original Stan data passed to the model.
Required for \code{method = "analytic_glmm"}.  Must contain \code{X}, \code{y},
\code{group} (1-indexed integer vector), and optionally \code{beta_vars} if the
beta column names differ from \code{"beta[1]"}, \code{"beta[2]"}.}

\item{beta_vars}{Character vector of fixed-effect column names in \code{chain}
(default \code{c("beta[1]", "beta[2]")}).}

\item{n_subsample}{Number of chain draws at which to evaluate A (default
500).  Draws are sampled uniformly without replacement.}
}
\value{
An S3 object of class \code{fibr_connection}. See
\code{\link[=print.fibr_connection]{print.fibr_connection()}} and \code{\link[=plot.fibr_connection]{plot.fibr_connection()}}.
}
\description{
Estimates the Ehresmann connection on the fiber bundle \eqn{E \to B}
induced by the posterior Fisher metric.  At each subsampled chain draw
\eqn{t}, the J x K connection matrix
\deqn{A(t) = -G_{FF}(t)^{-1} G_{BF}(t)}
gives the horizontal lift: if the base moves by \eqn{d\theta}, the fiber
must move by \eqn{A(t)\,d\theta} to stay horizontal.

Currently supports \code{method = "analytic_glmm"} for the centred two-level
logistic GLMM specified in \code{inst/stan/glmm_centred.stan}.
}
