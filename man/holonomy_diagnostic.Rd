% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/holonomy.R
\name{holonomy_diagnostic}
\alias{holonomy_diagnostic}
\title{Holonomy diagnostic for hierarchical MCMC chains}
\usage{
holonomy_diagnostic(
  chain,
  base_vars,
  fiber_vars,
  epsilon = NULL,
  n_bootstrap = 200L,
  min_gap = 50L,
  k = 100L,
  max_loops = 5000L,
  weights = c("distance", "uniform"),
  ridge = 1e-06,
  residualize_fiber = TRUE
)
}
\arguments{
\item{chain}{A \code{\link[posterior:draws_array]{posterior::draws_array}} (recommended), \code{draws_matrix},
or a plain numeric matrix with named columns.  Rows are iterations,
columns are parameters.}

\item{base_vars}{Character vector naming the base-space (hyperparameter)
columns, e.g. \code{c("mu", "sigma")}.}

\item{fiber_vars}{Character vector naming the fiber (group-level) parameter
columns, e.g. \code{paste0("alpha[", 1:8, "]")}.}

\item{epsilon}{Loop-detection tolerance in standardised base-space units.
\code{NULL} (default) auto-selects per chain.}

\item{n_bootstrap}{Bootstrap resamples for eigenvalue uncertainty (default 200).}

\item{min_gap}{Minimum iterations between loop start and end (default 50).}

\item{k}{Nearest neighbours queried per point in loop detection (default 100).}

\item{max_loops}{Maximum loop pairs per chain (default 5000).}

\item{weights}{Loop weighting for the transport regression; \code{"distance"}
(default) or \code{"uniform"}. See \code{\link[=estimate_transport_map]{estimate_transport_map()}}.}

\item{ridge}{Ridge penalty for the transport map Gram matrix (default \code{1e-6}).}

\item{residualize_fiber}{If \code{TRUE} (default), fiber draws are residualised
against the base draws via OLS before transport estimation.  This removes
the linear effect of the base on the fiber (e.g. the mean-shift
\code{alpha ~ mu} in a centered GLMM), isolating the true vertical component
of the fiber bundle where geometric holonomy lives.  Set to \code{FALSE} to
operate on raw fiber coordinates.}
}
\value{
An S3 object of class \code{fibr_holonomy}. See \code{\link[=print.fibr_holonomy]{print.fibr_holonomy()}}
and \code{\link[=plot.fibr_holonomy]{plot.fibr_holonomy()}} for display methods.  The list contains:
\describe{
\item{H}{Estimated \eqn{J \times J} transport matrix.}
\item{eigenvalues}{Complex eigenvalues of \code{H}, sorted by decreasing modulus.}
\item{boot_eigenvalues}{Complex matrix of bootstrapped eigenvalues.}
\item{frobenius_dev}{\eqn{\|H - I\|_F}.}
\item{n_loops}{Total loop pairs used.}
\item{loops}{Data frame of all detected loop pairs.}
\item{residualized}{Logical; whether fiber was residualised against base.}
\item{call, base_vars, fiber_vars}{Metadata.}
}
}
\description{
Estimates the holonomy transport map \eqn{\hat{H}} from an MCMC chain on
a hierarchical model.  Non-trivial holonomy --- eigenvalues of \eqn{\hat{H}}
away from 1 --- indicates that the fiber bundle structure of the parameter
space is creating a geometric mixing obstruction invisible to standard
diagnostics (R-hat, ESS, divergences).

Loop detection and transport estimation run \strong{per chain} so that
independent chains are never spliced into a single trajectory.  Loop pairs
from all chains are pooled for the final regression.
}
