% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/smoothbp_advisor.R
\name{smoothbp_advisor}
\alias{smoothbp_advisor}
\title{Parameterisation advisor for smoothbp changepoint random effects}
\usage{
smoothbp_advisor(fit, n_draws = 200L, threshold_nc = 0.6, threshold_c = 0.4)
}
\arguments{
\item{fit}{A \code{smoothbp_fit} from \code{smoothbp::smoothbp()} or
\code{smoothbp::smoothbp_ss()}, with at least one omega random effect
(\code{omega = list(~ 1 + (1 | group))}).}

\item{n_draws}{Number of posterior draws to evaluate the metric at
(default 200; subsampled uniformly).}

\item{threshold_nc}{Prior fraction above which non-centred is recommended
(default 0.60).}

\item{threshold_c}{Prior fraction below which centred is safe
(default 0.40).}
}
\value{
An S3 object of class \code{fibr_smoothbp_advice}.  Contains one list
element per breakpoint that has omega random effects, each with
\code{prior_frac_mean}, \code{prior_frac_q05}, \code{prior_frac_q95},
\code{recommendation}, and \code{delta} (one entry per group).  \code{delta[j]} is the
recommended per-group NC mixing fraction
\eqn{\delta_j = \sqrt{\text{prior\_frac}_j}}.
Print and plot methods included.
}
\description{
For each random effect on a changepoint location (\code{omega_k_g} = per-group
deviation from the population changepoint at breakpoint k), computes the
\strong{Fisher information decomposition} at a subsample of posterior draws.

The key quantity is \code{prior_frac}:

\deqn{\text{prior\_frac}_{k,g} =
  \frac{G_\text{prior}}{G_\text{prior} + G_\text{lik}}}

where
\deqn{G_\text{prior} = \sigma_{\text{re},k}^{-2}, \quad
  G_\text{lik} = \sigma^{-2}
    \sum_{i:\,\text{group}=g}
      \!\!\!\left(\frac{\partial\mu_i}{\partial\omega_{k,i}}\right)^{\!2}}

\itemize{
\item \code{prior_frac} \eqn{\to 1}: prior dominates -- group changepoints are
poorly identified from data relative to the shrinkage prior.  The sampler
is in the funnel regime and non-centred reparameterisation would help.
\item \code{prior_frac} \eqn{\to 0}: likelihood dominates -- centred
parameterisation is efficient and mixing should be adequate.
\item Mixed: flag individual groups for attention.
}

\strong{What to do with the results:}

When \code{prior_frac} is high, re-fit with \code{reparameterise = "omega"}:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{fit_nc <- smoothbp(..., reparameterise = "omega")
fit_nc_ss <- smoothbp_ss(..., reparameterise = "omega")
}\if{html}{\out{</div>}}

This activates the non-centred HMC parameterisation in the Rust sampler:
\code{z[j] = beta_om[j] / sigma_re_om[k]} is sampled with an \code{N(0,1)} prior,
and \code{beta_om[j] = z[j] * sigma_re_om[k]} is reconstructed automatically.
The \code{sigma_re_om} Gibbs step and the stored draws are unchanged -- output
is in the original (centred) parameterisation for easy interpretation.

Additional options if \code{reparameterise = "omega"} is insufficient:
\enumerate{
\item Increase \code{warmup} and \code{iter}.
\item Check \code{fit$n_divergent} -- many divergences confirm a remaining funnel.
\item Fix the changepoint for poorly-identified groups using
\code{omega = list(fixed(value))}.
}

The \code{prior_frac} values quantify the severity: values above 0.8 indicate a
serious funnel; 0.6-0.8 suggests moderate difficulty worth addressing.

The gradient is computed analytically from the sigmoid smooth-transition
likelihood:

\deqn{\frac{\partial\mu_i}{\partial\omega_{k,i}} =
  -\Bigl[\delta_{k,i}\,\sigma_{ki}\bigl(1 + d_{ki}\,\rho_{ki}(1-\sigma_{ki})\bigr)
  + b_{1,i}\,\mathbf{1}_{k=1}\Bigr]}

where \eqn{d_{ki} = \tau_i - \omega_{k,i}} and
\eqn{\sigma_{ki} = \text{logistic}(d_{ki}\,\rho_{ki})}.
}
