% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/smoothbp_advisor.R
\name{as_smoothbp_re_fraction}
\alias{as_smoothbp_re_fraction}
\alias{as_smoothbp_re_fraction.fibr_smoothbp_advice}
\title{Convert smoothbp advisor output to re_fraction format}
\usage{
as_smoothbp_re_fraction(advice, ...)

\method{as_smoothbp_re_fraction}{fibr_smoothbp_advice}(advice, ...)
}
\arguments{
\item{advice}{A \code{fibr_smoothbp_advice} object returned by
\code{\link[=smoothbp_advisor]{smoothbp_advisor()}}.}

\item{...}{Unused.}
}
\value{
A named list of numeric vectors, compatible with
\code{smoothbp(..., re_fraction = ...)}.
}
\description{
Extracts the per-group, per-breakpoint NC mixing fractions (\code{delta}) from a
\code{fibr_smoothbp_advice} object and returns them in the list format expected
by the \code{re_fraction} argument of \code{\link[smoothbp:smoothbp]{smoothbp::smoothbp()}} and
\code{\link[smoothbp:smoothbp_ss]{smoothbp::smoothbp_ss()}}.
}
\details{
The returned list has one element per breakpoint that has omega random
effects.  Each element is a named numeric vector of \code{delta} values in
\eqn{[0, 1]}, one per group.  Groups with \code{delta > threshold} will use the
non-centred parameterisation; others use centred.  The threshold is applied
inside the smoothbp sampler (default 0.5).
}
\examples{
\dontrun{
advice  <- smoothbp_advisor(fit)
re_frac <- as_smoothbp_re_fraction(advice)
fit_nc  <- smoothbp(..., re_fraction = re_frac)
}

}
