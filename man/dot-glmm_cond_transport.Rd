% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/conditional_transport.R
\name{.glmm_cond_transport}
\alias{.glmm_cond_transport}
\title{Conditional (Laplace affine) transport between two base points}
\usage{
.glmm_cond_transport(alpha, lap_from, lap_to)
}
\arguments{
\item{alpha}{J-vector; current fiber draw.}

\item{lap_from}{Laplace object at the FROM base point (output of
\code{\link{.glmm_laplace}}).}

\item{lap_to}{Laplace object at the TO base point.}
}
\value{
Named list:
\describe{
\item{alpha_new}{J-vector; transported fiber draw.}
\item{log_jac}{Scalar; \eqn{\sum_j \log(s^{\rm to}/s^{\rm from})}. Add
to the log acceptance ratio.}
}
}
\description{
Applies the map
\deqn{T(\alpha)_j = m_j^{\rm to} + \frac{s_j^{\rm to}}{s_j^{\rm from}}
  (\alpha_j - m_j^{\rm from})}
and accumulates the log-Jacobian \eqn{\sum_j \log(s_j^{\rm to}/s_j^{\rm from})}.
}
\details{
The map reduces to non-centering when there is no data (\eqn{s_j \to \sigma}),
to the identity as \eqn{\sigma \to \infty}, and is well-behaved as
\eqn{\sigma \to 0} (unlike the Fisher-metric lift).
}
\keyword{internal}
