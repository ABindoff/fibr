% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/conditional_transport.R
\name{.glmm_laplace}
\alias{.glmm_laplace}
\title{Newton mode and scale for the per-group Laplace conditional}
\usage{
.glmm_laplace(theta, beta, stan_data, m_init = NULL)
}
\arguments{
\item{theta}{2-vector \eqn{(\mu, \log\sigma)}.}

\item{beta}{K-vector of fixed-effect coefficients.}

\item{stan_data}{Named list with \code{X} (N×K), \code{y} (N), \code{group}
(N, integer 1..J).}

\item{m_init}{Optional J-vector warm start for Newton (default: all \eqn{\mu}).}
}
\value{
Named list:
\describe{
\item{m}{J-vector; per-group conditional modes.}
\item{s}{J-vector; per-group Laplace scales (\eqn{G_j^{-1/2}}).}
\item{G}{J-vector; per-group conditional precisions.}
}
}
\description{
Finds the mode \eqn{m_j} of \eqn{l_j(\alpha_j)} — the per-group conditional
log-density — by Newton's method, and returns the Laplace scale
\eqn{s_j = G_j^{-1/2}} where \eqn{G_j = 1/\sigma^2 + s_j^{\rm lik}(m_j)}.
}
\details{
Log-concavity of \eqn{l_j} guarantees a unique root and global convergence
of Newton with step-halving.
}
\keyword{internal}
