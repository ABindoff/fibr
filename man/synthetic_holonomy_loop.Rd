% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/curvature.R
\name{synthetic_holonomy_loop}
\alias{synthetic_holonomy_loop}
\title{Linearised holonomy along a synthetic circular loop in base space}
\usage{
synthetic_holonomy_loop(
  connection,
  mu0 = NULL,
  sigma0 = NULL,
  alpha0 = NULL,
  beta0 = NULL,
  radius = 0.3,
  n_steps = 200L
)
}
\arguments{
\item{connection}{A \code{fibr_connection} object.}

\item{mu0, sigma0}{Centre of the circle in base space.  Defaults to the
posterior mean of \code{mu} and \code{sigma} from the connection's subsampled draws.}

\item{alpha0}{Fiber point at which to evaluate the linearised connection
(J-vector).  Defaults to the posterior mean of alpha.}

\item{beta0}{Fixed-effect coefficients (2-vector).  Defaults to posterior
mean from the subsampled draws.}

\item{radius}{Radius of the circle in raw \eqn{(\mu, \sigma)} units
(default 0.3, roughly 0.3 posterior SDs for typical hierarchical models).}

\item{n_steps}{Number of discretisation steps around the circle (default 200).}
}
\value{
A data frame with one row per group j and columns:
\describe{
\item{j}{Group index.}
\item{H_numerical}{Holonomy scalar from integrating the connection:
\eqn{1 + \oint A_j\,d\theta}.}
\item{H_stokes}{Stokes approximation: \eqn{\exp(F_j \cdot \pi r^2)}.}
\item{F_j}{Curvature at the centre point.}
\item{A_mu, A_sigma}{Connection coefficients at the centre.}
}
}
\description{
Constructs a circular path in \eqn{(\mu, \sigma)} space and integrates the
\strong{linearised} connection form along it (fiber \eqn{\bm{\alpha}} held fixed
at \code{alpha0}, equivalently \eqn{G_{FF}} frozen at the loop centre).  The
result is compared with the first-order Stokes approximation
\eqn{H_j = 1 + F_j^{\text{lin}} \cdot \pi r^2 / \alpha_{0j}}.

\strong{This function does not integrate the true (full) Ehresmann connection.}
The true connection is flat (full curvature identically zero;
see \code{data-raw/verify_flat_connection.R}), so its holonomy over any loop is
trivial.  What this function computes is the holonomy of the
\emph{fiber-frozen} linearisation, which is the object validated in Figure 3
of the companion paper and whose curvature is
\eqn{F_j^{\text{lin}} = -2/(\sigma^5 G_{FF,j}^2)}.
}
