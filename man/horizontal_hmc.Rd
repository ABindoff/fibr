% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/horizontal_sampler.R
\name{horizontal_hmc}
\alias{horizontal_hmc}
\title{Horizontal leapfrog HMC for the centred GLMM}
\usage{
horizontal_hmc(
  stan_data,
  n_iter = 2000L,
  n_warmup = 1000L,
  n_chains = 4L,
  L = 10L,
  epsilon = 0.1,
  transport = c("reparam", "fisher_legacy"),
  target_rate = 0.65,
  init = NULL,
  seed = NULL,
  verbose = TRUE
)
}
\arguments{
\item{stan_data}{Named list with X (N×2), y (N), group (N, 1-indexed).}

\item{n_iter}{Post-warmup iterations per chain (default 2000).}

\item{n_warmup}{Warmup iterations per chain (default 1000).}

\item{n_chains}{Number of independent chains (default 4).}

\item{L}{Leapfrog steps per trajectory (default 10 for reparam;
use \eqn{L = 1} for fisher_legacy MALA regime).}

\item{epsilon}{Initial step size (default 0.10). Adapted during warmup.}

\item{transport}{Character; one of \code{"reparam"} (default) or
\code{"fisher_legacy"}.}

\item{target_rate}{Target Metropolis acceptance rate (default 0.65).}

\item{init}{Optional named list: \code{mu}, \code{sigma},
\code{alpha}, \code{beta}.  Defaults to prior draws.}

\item{seed}{Random seed.}

\item{verbose}{Print per-chain progress (default \code{TRUE}).}
}
\value{
A \code{\link[posterior]{draws_array}} with the same variable
naming as the centred Stan model: \code{mu}, \code{sigma},
\code{alpha[1]}, \ldots, \code{beta[2]}.
}
\description{
Two transport strategies, selected by \code{transport}:

\strong{Reparameterised HMC} (\code{transport = "reparam"}, default): exact HMC in
standardised z-coordinates \eqn{z_j = (\alpha_j - m_j)/s_j}, where
\eqn{(m_j, s_j)} is the per-group Laplace approximation.  The target
density is \eqn{\tilde\ell = \ell(\alpha(z)) + \sum_j \log s_j}.
Identity mass matrix; standard leapfrog; gradients via chain rule through
the Laplace implicit-function derivatives.  Newton solver warm-started
across leapfrog steps.

\strong{Fisher legacy} (\code{transport = "fisher_legacy"}): connection-corrected HMC
using a fixed diagonal Fisher metric \eqn{G_d} and horizontal correction
\eqn{A(\Delta\mu, \Delta\sigma)} applied at every leapfrog step.  Kept for
SBC failure-mode comparison.  Recommended \eqn{L = 1} (MALA regime).
}
