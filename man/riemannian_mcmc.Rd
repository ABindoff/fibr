% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rmhmc.R
\name{riemannian_mcmc}
\alias{riemannian_mcmc}
\title{Riemannian HMC for the centred GLMM}
\usage{
riemannian_mcmc(
  stan_data,
  n_iter = 2000L,
  n_warmup = 1000L,
  n_chains = 4L,
  L = 10L,
  epsilon = 0.1,
  method = c("diagonal", "softabs"),
  softabs_alpha = 1,
  target_rate = 0.65,
  init = NULL,
  seed = NULL,
  verbose = TRUE
)
}
\arguments{
\item{stan_data}{Named list with X (N×2), y (N), group (N, 1-indexed).}

\item{n_iter}{Post-warmup iterations per chain (default 2000).}

\item{n_warmup}{Warmup iterations per chain (default 1000).}

\item{n_chains}{Number of independent chains (default 4).}

\item{L}{Leapfrog steps per trajectory (default 10).}

\item{epsilon}{Initial step size (default 0.1). Adapted during warmup.}

\item{method}{Metric to use: \code{"diagonal"} (default, always PD, fast) or
\code{"softabs"} (SoftAbs regularised full metric; captures off-diagonal coupling).}

\item{softabs_alpha}{SoftAbs smoothness parameter alpha (default 1).
Larger values give a sharper approximation to the absolute-value metric.}

\item{target_rate}{Target Metropolis acceptance rate for adaptive tuning
(default 0.65). For \code{L = 1} (MALA) use 0.57.}

\item{init}{Optional named list: \code{mu}, \code{sigma}, \code{alpha}, \code{beta}.
Defaults to prior draws.}

\item{seed}{Random seed.}

\item{verbose}{Print per-chain progress (default \code{TRUE}).}
}
\value{
A \code{\link[posterior:draws_array]{posterior::draws_array}} with the same variable naming as the
centred Stan model: \code{mu}, \code{sigma}, \code{alpha[1]}, ..., \code{beta[2]}.
}
\description{
HMC with a \strong{per-trajectory locally-adapted mass matrix}: the full
\eqn{P \times P} Fisher metric \eqn{G(q)} is computed at the start of each
trajectory and held fixed for \eqn{L} leapfrog steps.  This is exactly
correct (detailed balance holds) because:
\enumerate{
\item Momentum is sampled from \eqn{N(0, G(q_\text{start}))} — density cancels
in the M-H ratio.
\item The leapfrog with FIXED \eqn{G^{-1}} as the mass matrix is
volume-preserving.
\item Both \eqn{H_\text{start}} and \eqn{H_\text{end}} use the same \eqn{G},
so the acceptance ratio is exact.
}

The gain over Euclidean HMC: \eqn{G(q)} is adapted to the local geometry —
the funnel in \eqn{\sigma}, the varying identifiability of each
\eqn{\alpha_j}, and the coupling between base and fiber directions captured
by the off-diagonal blocks.
}
