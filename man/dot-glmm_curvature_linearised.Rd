% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/connection_glmm.R
\name{.glmm_curvature_linearised}
\alias{.glmm_curvature_linearised}
\title{Linearised curvature of the GLMM connection}
\usage{
.glmm_curvature_linearised(G_FF_diag, sigma)
}
\arguments{
\item{G_FF_diag}{J-vector returned by \code{\link[=.glmm_G_FF]{.glmm_G_FF()}}.}

\item{sigma}{Scalar; group-level SD.}
}
\value{
J-vector.  All values are negative (\eqn{F_j < 0}).
}
\description{
Computes the base-derivative part of the Ehresmann curvature 2-form, with
the fiber coordinate \eqn{\alpha} (equivalently \eqn{G_{FF}}) held fixed:
}
\details{
\deqn{F_j^{\text{lin}} = \partial_\mu A_{j,\sigma} - \partial_\sigma A_{j,\mu}
        = -2 / (\sigma^5 G_{FF,j}^2)}

\strong{This is NOT the full Ehresmann curvature.}  The full curvature of
\eqn{A = -G_{FF}^{-1}G_{BF}} is identically zero for this model class:
the fiber-derivative (vertical) terms \eqn{A_\mu \partial_\alpha A_\sigma -
A_\sigma \partial_\alpha A_\mu = +2/(\sigma^5 G_{FF}^2)} cancel the base
terms exactly, so the connection is flat (see Proposition \code{prop:flat} in the
companion paper and \code{data-raw/verify_flat_connection.R}).

This function returns the linearised quantity because that is what
\code{\link[=synthetic_holonomy_loop]{synthetic_holonomy_loop()}} integrates (the fiber is frozen at the loop
centre), and it equals \eqn{2\pi_j^2/\sigma} in absolute value, making
it useful as a proxy for where the prior fraction \eqn{\pi_j} is large.
}
\keyword{internal}
