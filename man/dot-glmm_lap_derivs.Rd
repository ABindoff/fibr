% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/conditional_transport.R
\name{.glmm_lap_derivs}
\alias{.glmm_lap_derivs}
\title{Implicit-function derivatives of the Laplace mode and log-scale}
\usage{
.glmm_lap_derivs(theta, beta, stan_data, lap)
}
\arguments{
\item{theta}{2-vector \eqn{(\mu, \log\sigma)}.}

\item{beta}{K-vector.}

\item{stan_data}{Named list with \code{X}, \code{y}, \code{group}.}

\item{lap}{Output of \code{\link{.glmm_laplace}} at the same
\code{(theta, beta)}.}
}
\value{
Named list of J-vectors (and J×K matrices for beta components):
\code{dm_dmu}, \code{dm_dsig}, \code{dm_db} (J×K),
\code{dls_dmu}, \code{dls_dsig}, \code{dls_db} (J×K).
Multiply \code{*_dsig} by \eqn{\sigma} for the \eqn{\log\sigma} direction.
}
\description{
At \eqn{a = m_j} (the Laplace mode), the implicit function theorem gives
exact partial derivatives of \eqn{m_j(\theta, \beta)} and
\eqn{\log s_j(\theta, \beta)} with respect to all base parameters.
}
\details{
Derivatives with respect to \eqn{\log\sigma} follow by the chain rule:
\eqn{d/d(\log\sigma) = \sigma \cdot d/d\sigma}.
}
\keyword{internal}
